import { AppWrap } from "../wrapper";

const GraphicsDesigns = AppWrap(() => {
  return (
    <>
      <div>
        GraphicsDesigns
      </div>

    </>
  )
}, 'graphicsDesigns');

export default GraphicsDesigns